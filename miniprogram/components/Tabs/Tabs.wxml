<!--components/Tabs/Tabs.wxml-->

<view class="tabs">
	<view class="tabs_title">
    <view wx:for= "{{parmas}}" wx:key="id" class="tabs_title_item {{item.isactive ? 'active':''}}" bindtap="changeTap" data-index="{{index}}">
    {{item.name}}
    </view>
	</view>
	<view class="tabs_content">
		内容
	</view>

</view>